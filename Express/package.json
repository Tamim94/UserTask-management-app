{
  "name": "express",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "start": "cross-env NODE_ENV=development node ./bin/www",
    "dev": " cross-env NODE_ENV=development nodemon ./bin/www",
    "build": "cd ../frontend && npm install && npm run build && cp -r ../frontend/dist ./public"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "bcryptjs": "^2.4.3",
    "config": "^3.3.11",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "debug": "~2.6.9",
    "dotenv": "^16.4.5",
    "express": "~4.16.1",
    "express-validator": "^7.0.1",
    "http-errors": "~1.6.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.1",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^2.0.20"
  }
}
